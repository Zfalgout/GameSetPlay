<% provide(:title, 'All users') %>

<%= will_paginate %>

<div class ="jumbotron">
	<h1>All users</h1>

	<ul class="users">
	    <% @users.each do |user| %>
		    <li>
		      <%= gravatar_for user%>
		      <%= link_to user.name, user %>
		    </li>
		<% end %>
	</ul>

<%= will_paginate %>

</div>